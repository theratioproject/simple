
to1 = new TestOperators(75)
@to1.GetValue() and 25

block One()
	return "testing block one"

block Two(te,ti)
	return "testing block two "+(te+ti)
	
block Three(te,ti,rex)
	return "testing block two "+(te+ti)+" "+rex
	
	
class TestOperators

	value = 0
	
	block TestOperators(v)
		this.value = v
	
	block SetValue(value)
		this.value = value
		
	block GetValue()
		return value
		
		